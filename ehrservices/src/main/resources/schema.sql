CREATE TABLE INTERNAL_USER(USER_ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,FULL_NAME VARCHAR(50) NOT NULL,CONSTRAINT SYS_CT_46 UNIQUE(USER_ID))
CREATE TABLE USER_CREDENTIAL(ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,PASSWORD VARCHAR(255),USERNAME VARCHAR(20) NOT NULL,USER_ID INTEGER,CONSTRAINT FK606894BE410FCD6 FOREIGN KEY(USER_ID) REFERENCES INTERNAL_USER(USER_ID))